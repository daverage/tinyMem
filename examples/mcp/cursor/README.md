# Cursor MCP Example

Cursor supports connecting to local MCP servers via an `mcp.json` file.

How to use:
1. Copy `mcp.json` to your Cursor MCP configuration location.
2. Ensure `tinymem` is on your `PATH`, or change `command` to an absolute path.
3. Restart Cursor.
