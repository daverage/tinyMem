{
  "$schema": "https://raw.githubusercontent.com/charmbracelet/crush/main/schema.json",
  "mcp": {
    "tinymem": {
      "type": "stdio",
      "command": "tinymem",
      "args": ["mcp"],
      "timeout": 120,
      "env": {
        "TINYMEM_METRICS_ENABLED": "true",
        "TINYMEM_LOG_LEVEL": "info",
        "TINYMEM_SEMANTIC_ENABLED": "false",
        "TINYMEM_RECALL_MAX_ITEMS": "20",
        "TINYMEM_RECALL_MAX_TOKENS": "4000"
      },
      "disabled_tools": []
    }
  },
  "system_prompt_suffix": "\n\nYou have access to a persistent memory system via tinyMem. IMPORTANT: Before answering any question about this project, first use memory_query to check for relevant context. After making decisions or learning important information, use memory_write to record it for future sessions."
}
